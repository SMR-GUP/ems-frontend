{
  "dependencies": {
    "dotenv": "^16.4.5",
    "react-datepicker": "^4.18.0",
    "react-toastify": "^10.0.4"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"cd frontend && npm run dev\" \"cd server && npm start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
